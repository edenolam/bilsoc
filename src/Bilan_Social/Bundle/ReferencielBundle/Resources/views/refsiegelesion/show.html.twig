{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('refsiegelesion_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refSiegeLesion.cdSiegelesi }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refSiegeLesion.lbSiegelesi }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refSiegeLesion.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refSiegeLesion} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('refsiegelesion_edit', { 'idSiegelesi': refSiegeLesion.idSiegelesi }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}

    </div>
{% endblock %}

