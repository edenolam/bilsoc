{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('reforganismeformation_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refOrganismeFormation.cdOrgaform }}</td>
                </tr>
                <tr>
                    <th>libellé</th>
                    <td>{{ refOrganismeFormation.lbOrgaform }}}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refOrganismeFormation.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refOrganismeFormation} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('reforganismeformation_edit', { 'idOrgaform': refOrganismeFormation.idOrgaform }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}



        </ul>
    {% endblock %}
