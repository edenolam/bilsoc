{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <h1>Liste des types de CDD </h1>
        <div class="clearfix">
            <a class="btn button pull-right margin10" href="{{ path('reftypecdd_new') }}">Ajouter une donnée au référentiel</a>
            <a class="btn button pull-right margin10" href="{{ path('csv_export_reftypecdd') }}">export csv</a>
            <a class="btn button margin10" href="{{ path('referenciel_homepage') }}">Retour à la liste des référentiels</a>
        </div>
        <table class="table table-striped" id="index">
            <thead>
                <tr>
                    <th>Code</th>
                    <th>Libellé</th>
                    <th>Archivé</th>
                    {% include '@Referenciel/indexListRefSharedFieldsTheadLayout.html.twig' %}
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for refTypeCdd in refTypeCdds %}
                    <tr>
                        <td><a href="{{ path('reftypecdd_show', { 'idTypeCdd': refTypeCdd.idTypeCdd }) }}">{{ refTypeCdd.cdTypecdd }}</a></td>
                        <td>{{ refTypeCdd.lbTypeCdd }}</td>
                        <td>{% if refTypeCdd.blVali %}Oui{% else %}Non{% endif %}</td>
                        {% include '@Referenciel/indexListRefSharedFieldsTbodyLayout.html.twig' with {'ref':refTypeCdd} %}
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('reftypecdd_show', { 'idTypeCdd': refTypeCdd.idTypeCdd }) }}"><i class="fa fa-search" aria-hidden="true"></i></a>
                                </li>
                                <li>
                                    <a href="{{ path('reftypecdd_edit', { 'idTypeCdd': refTypeCdd.idTypeCdd }) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}


