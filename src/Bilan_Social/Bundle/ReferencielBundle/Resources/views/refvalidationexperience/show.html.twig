{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('refvalidationexperience_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refValidationExperience.cdEbcf }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refValidationExperience.lbEbcf }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refValidationExperience.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refValidationExperience} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('refvalidationexperience_edit', { 'idEbcf': refValidationExperience.idEbcf }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}



        </ul>
    {% endblock %}