{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('reftrancheage_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refTrancheAge.cdTranage }}</td>
                </tr>
                <tr>
                    <th>libellé</th>
                    <td>{{ refTrancheAge.lbTranage }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refTrancheAge.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refTrancheAge} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('reftrancheage_edit', { 'idTranage': refTrancheAge.idTranage }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}



        </ul>
    {% endblock %}


