{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('refcategorieboeth_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refCategorieBoeth.cdCategorieboeth }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refCategorieBoeth.lbCategorieboeth }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refCategorieBoeth.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refCategorieBoeth} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('refcategorieboeth_edit', { 'idCategorieboeth': refCategorieBoeth.idCategorieboeth }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}

    </div>
{% endblock %}

