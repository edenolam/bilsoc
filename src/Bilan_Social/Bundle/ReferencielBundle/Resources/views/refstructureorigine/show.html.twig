{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('refstructureorigine_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refStructureOrigine.cdStruorig }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refStructureOrigine.lbStruorig }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refStructureOrigine.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refStructureOrigine} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('refstructureorigine_edit', { 'idStruorig': refStructureOrigine.idStruorig }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}



        </ul>
    {% endblock %}

