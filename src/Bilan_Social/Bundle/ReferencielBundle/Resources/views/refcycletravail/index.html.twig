{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <h1>Liste des cycles de travail</h1>
        <div class="clearfix">
            <a class="btn button pull-right margin10" href="{{ path('refcycletravail_new') }}">Ajouter une donnée au référentiel</a>
            <a class="btn button pull-right margin10" href="{{ path('csv_export_RefCycleTravail') }}">export csv</a>
            <a class="btn button margin10" href="{{ path('referenciel_homepage') }}">Retour à la liste des référentiels</a>
        </div>
        <table class="table table-striped" id="index">
            <thead>
                <tr>
                    <th>Code</th>
                    <th>Libellé</th>
                    <th>Groupe</th>
                    <th>Archivé</th>
                    {% include '@Referenciel/indexListRefSharedFieldsTheadLayout.html.twig' %}
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for refCycleTravail in refCycleTravails %}
                    <tr>
                        <td><a href="{{ path('refcycletravail_show', { 'idCycltrav': refCycleTravail.idCycltrav }) }}">{{ refCycleTravail.cdCycltrav }}</a></td>
                        <td>{{ refCycleTravail.lbCycltrav }}</td>
                        <td>{{ refCycleTravail.lbGroupeCycltrav }}</td>
                        <td>{% if refCycleTravail.blVali %}Oui{% else %}Non{% endif %}</td>
                        {% include '@Referenciel/indexListRefSharedFieldsTbodyLayout.html.twig' with {'ref':refCycleTravail} %}
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('refcycletravail_show', { 'idCycltrav': refCycleTravail.idCycltrav }) }}"><i class="fa fa-search" aria-hidden="true"></i></a>
                                </li>
                                <li>
                                    <a href="{{ path('refcycletravail_edit', { 'idCycltrav': refCycleTravail.idCycltrav }) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

