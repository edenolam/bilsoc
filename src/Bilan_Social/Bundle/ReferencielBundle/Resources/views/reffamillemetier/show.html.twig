{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
<div class="col-lg-9" >
    <a class="btn button pull-right margin10" href="{{ path('reffamillemetier_index') }}">Retour à la liste</a>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ refFamilleMetier.cdFamilleMetier }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ refFamilleMetier.lbFamilleMetier }}</td>
            </tr>

            <tr>
                <th>Domaine professionnel</th>
                <td>{{ refFamilleMetier.refDomaineProfessionnel.lbDomaineProfessionnel }}</td>
            </tr>
            <tr>
                <th>Archivé</th>
                <td>{% if refFamilleMetier.blVali %}Oui{% else %}Non{% endif %}</td>
            </tr>
            {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refFamilleMetier} %}

        </tbody>
    </table>
            <a class="btn button pull-right margin10" href="{{ path('reffamillemetier_edit', { 'idFamilleMetier': refFamilleMetier.idFamilleMetier }) }}">Modifier</a>

    {{ form_start(delete_form) }}

    {{ form_end(delete_form) }}

</div>
{% endblock %}
