{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
<div class="col-lg-9" >
    <a class="btn button pull-right margin10" href="{{ path('refdomainespecialite_index') }}">Retour à la liste</a>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ refDomaineSpecialite.cdDomaineSpecialite }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ refDomaineSpecialite.lbDomaineSpecialite }}</td>
            </tr>
            <tr>
                <th>Archivé</th>
                <td>{% if refDomaineSpecialite.blVali %}Oui{% else %}Non{% endif %}</td>
            </tr>
            {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refDomaineSpecialite} %}

        </tbody>
    </table>

            <a class="btn button pull-right margin10" href="{{ path('refdomainespecialite_edit', { 'idDomaineSpecialite': refDomaineSpecialite.idDomaineSpecialite }) }}">Modifier</a>

            {{ form_start(delete_form) }}
{#                <input type="submit" value="Delete">#}
            {{ form_end(delete_form) }}

{% endblock %}