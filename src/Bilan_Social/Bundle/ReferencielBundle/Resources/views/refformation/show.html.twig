{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('refformation_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refFormation.cdForm }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refFormation.lbForm }}</td>
                </tr>
                {#<tr>
                    <th>Prévention</th>
                    <td>{% if refFormation.blPrev %}Oui{% else %}Non{% endif %}</td>
                </tr>#}
                <tr>
                    <th>Archivé</th>
                    <td>{% if refFormation.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refFormation} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('refformation_edit', { 'idForm': refFormation.idForm }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}

    </div>
{% endblock %}
