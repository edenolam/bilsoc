{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('refinaptitudeboeth_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refInaptitudeBoeth.cdInaptitudeboeth }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refInaptitudeBoeth.lbInaptitudeboeth }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refInaptitudeBoeth.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refInaptitudeBoeth} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('refinaptitudeboeth_edit', { 'idInaptitudeboeth': refInaptitudeBoeth.idInaptitudeboeth }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}

    </div>
{% endblock %}

