{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('reftypemissionprevention_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refTypeMissionPrevention.cdTypemissprev }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refTypeMissionPrevention.lbTypemissprev }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refTypeMissionPrevention.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refTypeMissionPrevention} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('reftypemissionprevention_edit', { 'idTypemissprev': refTypeMissionPrevention.idTypemissprev }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}



        </ul>
    {% endblock %}