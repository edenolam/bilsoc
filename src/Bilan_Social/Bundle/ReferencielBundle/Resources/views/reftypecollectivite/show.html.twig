{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('reftypecollectivite_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refTypeCollectivite.cdTypecoll }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refTypeCollectivite.lbTypeColl }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refTypeCollectivite.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refTypeCollectivite} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('reftypecollectivite_edit', { 'idTypeColl': refTypeCollectivite.idTypeColl }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}



        </ul>
    {% endblock %}