{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <a class="btn button pull-right margin10" href="{{ path('refpourcentagetempapartiel_index') }}">Retour à la liste</a>

        <table class="table table-striped"
               <tbody>
                <tr>
                    <th>Code</th>
                    <td>{{ refPourcentageTempaPartiel.cdPourtemppart }}</td>
                </tr>
                <tr>
                    <th>Libellé</th>
                    <td>{{ refPourcentageTempaPartiel.lbPourtemppart }}</td>
                </tr>
                <tr>
                    <th>Nombre minutes borne min</th>
                    <td>{{ refPourcentageTempaPartiel.pcBorneMin }}</td>
                </tr>
                <tr>
                    <th>Nombre minutes borne Max</th>
                    <td>{{ refPourcentageTempaPartiel.pcBorneMax }}</td>
                </tr>
                <tr>
                    <th>Archivé</th>
                    <td>{% if refPourcentageTempaPartiel.blVali %}Oui{% else %}Non{% endif %}</td>
                </tr>
                {% include '@Referenciel/showRefSharedFieldsLayout.html.twig' with {'ref':refPourcentageTempaPartiel} %}
            </tbody>
        </table>


        <a class="btn button pull-right margin10" href="{{ path('refpourcentagetempapartiel_edit', { 'idPourtemppart': refPourcentageTempaPartiel.idPourtemppart }) }}">Modifier</a>

        {{ form_start(delete_form) }}
        
        {{ form_end(delete_form) }}



        </ul>
    {% endblock %}

