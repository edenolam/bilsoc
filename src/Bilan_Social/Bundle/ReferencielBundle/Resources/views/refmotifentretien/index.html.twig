{% extends '@Referenciel/layout.html.twig' %}

{% block body %}
    {{parent()}}
    <div class="col-lg-9" >
        <h1>Liste des motifs d'entretien </h1>
        <div class="clearfix">
            <a class="btn button pull-right margin10" href="{{ path('refmotifentretien_new') }}">Ajouter une donnée au référentiel</a>
            <a class="btn button pull-right margin10" href="{{ path('csv_export_refmotifentretien') }}">export csv</a>
            <a class="btn button margin10" href="{{ path('referenciel_homepage') }}">Retour à la liste des référentiels</a>
        </div>
        <table class="table table-striped" id="index">
            <thead>
                <tr>
                    <th>Code</th>
                    <th>Libellé</th>
                    <th>Archivé</th>
                    {% include '@Referenciel/indexListRefSharedFieldsTheadLayout.html.twig' %}
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for refMotifEntretien in refMotifEntretiens %}
                    <tr>
                        <td><a href="{{ path('refmotifentretien_show', { 'idMotientr': refMotifEntretien.idMotientr }) }}">{{ refMotifEntretien.cdMotientr }}</a></td>
                        <td>{{ refMotifEntretien.lbMotientr }}</td>
                        <td>{% if refMotifEntretien.blVali %}Oui{% else %}Non{% endif %}</td>
                        {% include '@Referenciel/indexListRefSharedFieldsTbodyLayout.html.twig' with {'ref':refMotifEntretien} %}
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('refmotifentretien_show', { 'idMotientr': refMotifEntretien.idMotientr }) }}"><i class="fa fa-search" aria-hidden="true"></i></a>
                                </li>
                                <li>
                                    <a href="{{ path('refmotifentretien_edit', { 'idMotientr': refMotifEntretien.idMotientr }) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

